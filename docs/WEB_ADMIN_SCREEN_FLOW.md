# 0.8.0A Web 後台畫面流程

## 使用情境

第一版 Web 後台 MVP 的使用者視角是「廟方實際管理者」。目標不是逐欄位微調，而是先用截圖與操作回饋確認整體資訊架構、管理流程與畫面理解度。

第一版只做可視化畫面與流程驗證，不正式上線，不接正式 LINE Bot，不改正式 Google Sheets，不改 Render，不改 LINE Developers Webhook。

## 資料來源限制

畫面與流程只能使用：

- `中原福德宮_AppSheet_0612_正式啟用前備份_20260616`
- 本機 mock data
- 本機 dev adapter

不得使用正式主檔 `中原福德宮_AppSheet_0612` 作為 Web 後台 MVP 的寫入目標。

## 導覽結構

最新 UX 決策：左側選單只保留最上層模組，不列出細項功能。

- 主控台
- 善信管理
- 友宮管理
- 來訪 / 請帖
- 公告 / 活動
- 團隊管理
- 帳務管理
- 管理者設定

目前 `web_admin_mvp/` 已依 V2 架構文件先做成 9 個模組入口：

```text
主控台
善信管理
友宮管理
來訪 / 請帖
公告 / 活動
團隊管理
帳務管理
管理者設定
```

廟務職務與系統權限是團隊管理的下一層內容，不放在左側選單。仍只使用本機測試資料，不接正式 Google Sheets，不修改正式 LINE Bot runtime，不部署。

細項功能不攤在左側選單。一般模組主頁以列表、搜尋、篩選與查看詳情為主；`新增 / 編輯`、聯絡人、職務任期、廟務職務 / 系統權限、收支分類、發布管道設定、查詢維運與資料來源說明不應攤在左側選單或一般主頁快速卡片。

管理者設定集中放置：

- 標籤 / 主檔管理：發布管道、發布狀態、來訪型態、廟務職務、友宮聯絡人職稱、系統權限、帳務分類。
- 權限與角色：系統權限管理、管理者清單、`admin` 可轉移 / 增減 / 停用說明。
- 系統操作紀錄：最近查詢紀錄、查無資料紀錄、補資料建議 / 待補清單、LINE Bot 狀態說明。
- 系統與資料來源：開發資料來源說明、AppSheet 備援說明、正式主檔保護提醒、不影響正式 LINE Bot 說明。

本階段仍不做正式公告發送、自動推播、完整登入管理或正式權限驗證。

## 全站資料管理流程

所有主要資料管理模組共用以下流程：

```text
先瀏覽 / 搜尋 → 查看詳情 → 詳情頁內編輯 / 管理關聯資料
```

分工原則：

- 列表頁負責瀏覽、搜尋、篩選與查看詳情。
- 列表頁每筆資料的主要操作是 `查看詳情`。
- 列表頁不直接提供大量編輯、停用、刪除或管理關聯資料。
- 詳情頁才提供編輯、停用、刪除、管理聯絡人、管理關聯公告、管理權限、管理任期等操作。
- 新增 CTA 只保留在明確少數位置，不在列表與卡片中重複出現。
- 快速通道、列表與相關資料區塊不應重複呈現同一批資料。
- 各模組主頁不優先顯示大量快速卡片；若卡片只是重複列表功能，應移除。
- 設定類、權限類、系統操作紀錄、資料來源類集中到管理者設定。

## 模組語意邊界

- 友宮 = 宮廟主檔，用來管理友宮基本資料、聯絡資訊與主檔狀態。
- 來訪 / 請帖 = 友宮互動事件，用來記錄某間友宮在某一天的一次互動。
- 公告 / 活動 = 發布或活動內容，用來整理對外公告、活動資料與發布管道文字。
- 近期廟務動態 = 對人可讀的廟務事件，用於主控台快速掌握最近來訪、公告、活動、善信服務、團隊值勤等異動，不顯示系統 log。
- 系統操作紀錄 = 管理者維運資訊，包含查詢紀錄、查無資料、補資料建議與 LINE Bot 狀態說明，不作為一般使用者主要操作內容。

目前 prototype 應維持：

- 友宮、來訪 / 請帖、公告 / 活動、團隊管理、帳務管理主頁均以列表優先，列表操作以 `查看詳情` 為主。
- 編輯與關聯操作集中到各自詳情頁。
- 關聯資料在主資料詳情頁中只顯示數量、最近一筆或摘要，提供查看 CTA，不在摘要區直接編輯。
- 管理者設定集中處理設定、權限、系統操作紀錄與資料來源。
- 所有資料仍只使用本機測試資料，不接正式 Google Sheets，不影響正式 LINE Bot。

## 第三方驗證前 UX cleanup

`0.8.0A-4` 先做內部整理，再交給第三方驗證：

- 所有次頁、詳情頁與表單頁都要提供 `返回列表`、`返回上一頁` 或 `返回來源資料`。
- 管理者設定子區塊需提供 `返回管理者設定`。
- 不讓使用者只能依賴瀏覽器上一頁。
- 主要列表頁搜尋方式需一致：善信、友宮、來訪 / 請帖、公告 / 活動、帳務管理優先使用單一模糊搜尋列。
- 下拉選單可保留在表單或管理者設定，不作為一般列表頁的主要搜尋方式。
- 列表頁不放 `寫入狀態`、`資料說明`、敏感資訊提示等開發或安全說明欄位；這些內容歸到管理者設定或文件。
- 管理者設定只放系統參數、維運與規則說明，不放具體人員任期資料。
- 職務任期、值勤班表、廟務職務與人員資料歸在團隊管理。
- 標籤 / 主檔管理需分類呈現，不混用發布管道、發布狀態、廟務職務、友宮聯絡人職稱、系統權限與帳務分類。

## 第三方驗證前資訊架構 cleanup

`0.8.0A-6` 先清理明顯分類混亂，再交給第三方預覽：

- 主控台顯示 `近期廟務動態`，只放對人可讀的廟務事件。
- `友宮資料更新`、`帳務流水`、`善信紀錄`、`查無資料`、`補資料建議`、`LINE 查詢紀錄` 等系統 log 不放在主控台近期廟務動態。
- 系統 log 與操作紀錄歸到管理者設定的 `系統操作紀錄`。
- 友宮名稱只放宮廟主檔名稱，不混入 `參訪`、`祝壽`、`請帖`、`會香` 等事件描述。
- 最近互動可放在 `最近互動` 或摘要欄。
- 來訪 / 請帖是友宮互動事件；公告 / 活動是發布或活動內容。
- 來訪 / 請帖可以產生公告，但公告不是來訪主資料，也不取代來訪紀錄。

## 第三方預覽前 UI residue cleanup

`0.8.0A-7` 清理第三方預覽前仍殘留的畫面混淆：

- 來訪 / 請帖列表的 `友宮名稱` 欄只顯示宮廟主檔名稱。
- 來訪 / 請帖主題或最近互動描述放在獨立欄位，例如 `主題`、`最近互動` 或 `來訪主旨`。
- 一般列表頁不顯示型態主檔清單；來訪型態主檔只屬於 `管理者設定 → 標籤 / 主檔管理 → 來訪型態`。
- 一般使用者只在資料列或表單中看到已套用的來訪型態。
- 團隊管理主頁只保留團隊成員列表與值勤班表。
- `系統權限設定` 歸 `管理者設定 → 權限與角色 → 系統權限管理`。
- 查看個人的廟務職務、職務任期與系統權限，應放在團隊成員詳情頁，不放在團隊管理一般頁快速卡片。

## 1. 首頁 / 主控台

用途：讓管理者快速知道目前資料維護狀態。

建議內容：

- 友宮總數
- 近期來訪 / 請帖筆數
- 草稿或待確認公告數
- 近期廟務動態，例如來訪、公告、活動、善信服務或團隊值勤提醒

限制：

- 不做複雜 dashboard。
- 不做即時營運分析。
- 不連正式 LINE Bot 指標。

## 2. 友宮資料列表

用途：查找與維護友宮主資料。

建議功能：

- 模糊搜尋友宮名稱、主神、地址、關係狀態或最近互動
- 顯示主要聯絡人摘要
- 顯示最近互動摘要與日期
- 進入友宮詳情
- 進入新增友宮

主頁需以友宮資料列表為主，不放重複的友宮管理快速卡片。每筆資料操作只保留 `查看詳情`。

列表欄位可先保持精簡：

- 友宮名稱
- 地區
- 主祀神明
- 主要聯絡人
- 最近互動
- 狀態

## 3. 友宮資料詳情

用途：集中呈現單一友宮的主資料、聯絡人與互動紀錄。

建議區塊：

- 基本資料
- 聯絡資訊摘要
- 聯絡人列表
- 近期來訪 / 請帖
- 相關公告
- 內部備註

操作：

- 編輯友宮
- 管理聯絡人
- 新增來訪 / 請帖紀錄
- 查看相關公告
- 新增相關公告
- 停用友宮

關聯資料呈現原則：

- 聯絡人只顯示數量與最近摘要，完整資料進入聯絡人管理或詳情流程。
- 來訪 / 請帖只顯示數量與最近摘要，完整內容進入來訪詳情。
- 相關公告只顯示數量與最近摘要，完整內容進入公告詳情。

## 4. 新增 / 編輯友宮

用途：建立或修正友宮主資料。

建議欄位方向：

- 友宮名稱
- 別名
- 地區 / 地址
- 主祀神明
- 公開摘要
- 內部備註
- 是否公開顯示
- 是否僅限內部查詢

注意：

- 第一版先確認流程與欄位分組。
- 不直接改正式 `shrines` tab。
- Web 後台內部可使用 `temple` 命名，但需透過 adapter 對應現有 `shrines`。

## 5. 友宮聯絡人管理

用途：把友宮主資料與實際聯絡人拆開，避免所有資訊都塞在友宮主檔。

建議功能：

- 新增聯絡人
- 編輯聯絡人
- 標記主要聯絡人
- 停用不再使用的聯絡人
- 從職務下拉選單選擇聯絡人職稱

建議欄位：

- 所屬友宮
- 姓名
- 職務名稱
- 電話
- LINE 或其他聯絡方式
- 備註
- 是否主要聯絡人
- 是否啟用

職務名稱應盡量來自 `role_types`，避免自由輸入造成名稱混亂。

## 6. 來訪 / 請帖紀錄列表

用途：查詢所有來訪、請帖、邀請與廟際互動紀錄。

建議功能：

- 模糊搜尋友宮、日期、來訪型態、回覆狀態或備註
- 顯示紀錄狀態
- 進入新增 / 編輯紀錄

主頁需以來訪 / 請帖紀錄列表為主，不放選擇友宮、型態設定或待補資料快速卡片。每筆資料操作只保留 `查看詳情`。

建議欄位：

- 日期
- 友宮名稱
- 來訪型態
- 主題
- 地點
- 聯絡人
- 狀態

來訪次數不手動填，未來應由同一 `temple_id` 的 `temple_visits` 紀錄數自動計算。

## 7. 新增 / 編輯來訪紀錄

用途：建立來訪、請帖、邀請、祝壽、遶境等互動紀錄。

建議欄位：

- 友宮
- 日期
- 時間
- 地點
- 主題
- 來訪型態
- 方向
- 相關聯絡人
- 內容摘要
- 內部備註
- 是否需後續公告

來訪型態需支援多選，例如：

- 參訪
- 用餐
- 進香
- 邀請
- 請帖
- 祝壽
- 遶境
- 會香
- 聯誼
- 其他

## 8. 公告列表

用途：檢視公告草稿、已準備內容與未來可能發布的訊息。

建議功能：

- 模糊搜尋公告標題、狀態、日期、發布管道或關聯友宮
- 依日期排序
- 顯示是否關聯來訪紀錄
- 進入新增 / 編輯公告

公告 / 活動主頁需以公告列表與活動列表為主。上方不放關聯來訪紀錄或發布管道設定快速卡片。公告與活動每筆資料操作只保留 `查看詳情`。

限制：

- 第一版不做正式公告發送。
- 第一版不做自動推播。
- 第一版不接 LINE 主動推播。

## 9. 新增 / 編輯公告

用途：整理未來可能由 LINE 或其他管道發布的公告內容。

建議欄位：

- 標題
- 內容
- 狀態
- 活動日期
- 地點
- 相關來訪紀錄
- 相關活動
- 內部備註

未來 `announcements` 需能關聯 `temple_visits` 或 `events`，可採 `announcement_links` 或 `announcements.related_visit_id` 方向討論。

## 10. 團隊管理中的職務 / 權限檢視

用途：讓管理者看懂「廟務職務」與「系統權限」是兩件事。

建議內容：

- 成員列表
- 廟務職務
- 系統權限
- 任期
- 啟用狀態

職務 / 權限檢視是團隊管理的下一層內容，不放在左側選單。頁面需以名單列表為主，不放職稱主檔或系統權限說明快速卡片。每筆資料操作只保留 `查看詳情`。

重要原則：

- 使用者本人目前廟務職稱是總幹事，系統權限是 `admin`。
- `admin` 不應綁定單一個人。
- 未來需可轉移、增減、停用系統權限。

## 11. 善信管理

用途：管理一般善信、發財金、平安龜、還金 / 還願、善信本人可查詢紀錄，以及與帳務收入的關聯。

建議內容：

- 善信資料
- 發財金紀錄
- 平安龜紀錄
- 還金 / 還願紀錄
- 善信查詢紀錄
- 與帳務收入關聯

流程：

```text
善信建檔
→ 登記求發財金 / 平安龜
→ 年度紀錄
→ 還金 / 還願
→ 關聯帳務收入
→ LINE Bot 可查詢本人授權內容
```

權限原則：

- 善信本人只能查詢自己的授權紀錄。
- 團隊成員可協助查詢或登記部分資料，但不等於可管理帳務。
- 系統使用者依權限管理善信紀錄。
- 系統管理者維護分類、年度、權限與資料設定。

## 12. 團隊管理

用途：管理團隊成員、值勤班表、值勤週期、值勤提醒與節日 / 特殊日備註。

建議內容：

- 團隊成員
- 值勤班表
- 值勤週期
- 值勤提醒
- 節日 / 特殊日備註
- 與活動 / 來訪 / 公告關聯

重要原則：

- 團隊成員可以不是系統使用者。
- 值勤班表不等於職務任期。
- 值勤提醒可與 LINE 通知或公告有關，但本階段不做正式推播。

## 13. 帳務管理

用途：檢視未來帳務流水紀錄管理流程，不做完整會計、報稅、審計或銀行帳務功能。

命名原則：

- 未來正式模組名稱使用「帳務管理」。
- 功能描述使用「帳務流水紀錄」、「收支流水帳」、「月報公告草稿」。
- 避免使用容易被誤解為正式會計平台的命名。

建議內容：

- 帳務流水紀錄列表
- 模糊搜尋日期、收支、分類、項目、關聯來源、經手人或狀態
- 月報公告草稿

操作原則：

- 主頁只保留 `新增帳務流水` 作為主要 CTA。
- 帳務流水紀錄列表每筆只保留 `查看詳情`。
- 編輯帳務流水紀錄、儲存草稿、標記作廢、補備註與類別檢視放在帳務詳情頁。
- 收支分類與資料保護提醒集中在管理者設定。
- 帳務測試資料不得包含銀行帳號、帳戶、真實收據號碼、真實個資或敏感資訊。
- 帳務流水紀錄未來需支援關聯善信、發財金、平安龜、還金 / 還願、友宮來訪、活動或公告。
- 每月 LINE 群組公告是必要需求，但本階段只規劃月報公告草稿，不做正式推播。
- 現場表單需有顯性 `經手人 / 現場值班` 欄位，可由當日值勤班表預設，不應只依賴登入 session。
- 金額輸入未來需清洗全形數字、千分位逗號、中文單位、空白與非數字符號。
- 作廢不應直接刪除原始流水，也不以原金額乘以 -1 作為主要策略；應保留原紀錄、標示作廢，必要時另建沖銷紀錄。

月度公告對象至少包含：

- 團隊成員
- 管理委員會成員
- 系統管理者

權限原則：

- 團隊成員可接收必要的月度公告或摘要。
- 管理委員會成員可查看較完整流水帳摘要。
- 系統管理者可查看完整紀錄與設定公告範圍。
- 善信本人只可查詢與自己相關的授權紀錄，不可查看完整內部帳務流水。
- 公開公告與內部公告內容應分開。

## 14. 管理者設定

用途：集中設定類、權限類、系統操作紀錄與資料來源類內容。

固定區塊：

- 標籤 / 主檔管理：發布管道、發布狀態、來訪型態、廟務職務、友宮聯絡人職稱、系統權限、帳務分類。
- 權限與角色：系統權限管理、管理者清單、權限規則說明、`admin` 可轉移 / 增減 / 停用說明。
- 系統操作紀錄：最近查詢紀錄、查無資料紀錄、補資料建議 / 待補清單、LINE Bot 狀態說明。
- 系統與資料來源：開發資料來源說明、AppSheet 備援說明、正式主檔保護提醒、不影響正式 LINE Bot 說明、V2 temples / V1 shrines 對應、V2 temple_visits / V1 shrine_visits 對應、資料保護提醒。
- 未來可規劃健康燈號：Google Sheets 連線狀態、LINE Bot 狀態、Render 狀態、Web 後台資料來源狀態。本階段不實作真實連線檢查。

## 本輪補充限制

本輪只是架構補充：

- 不修改 Web 後台 prototype 畫面。
- 不修改正式 Google Sheets。
- 不修改 Render。
- 不修改 LINE Developers Webhook。
- 不修改正式 LINE Bot runtime。
- 不部署。
- 不 commit。

## 截圖回饋方式

第一版回饋建議以畫面截圖或簡短操作流程為主：

- 哪個畫面分類不直覺。
- 哪個流程不像廟方實際工作方式。
- 哪些欄位分組應調整。
- 哪些功能應延後，不應干擾第一版。

不建議在第一輪逐欄位微調，避免過早把 V1 欄位限制帶入 V2 後台設計。
